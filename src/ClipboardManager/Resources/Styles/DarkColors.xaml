<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Dark-mode color overrides.
        Merged AFTER Colors.xaml so these values shadow the light-mode defaults.
        Key names are identical to Colors.xaml — WPF's resource lookup finds the
        last definition in MergedDictionaries, which will be this dictionary.
    -->

    <!-- ── Dark surface colors ─────────────────────────────────────────── -->
    <Color x:Key="SurfaceColor">#FF1E1E2E</Color>
    <Color x:Key="SurfaceAltColor">#FF2A2A3E</Color>
    <Color x:Key="TextPrimaryColor">#FFEDEDED</Color>
    <Color x:Key="TextSecondaryColor">#FF9090A8</Color>
    <Color x:Key="BorderColor">#FF3A3A4A</Color>
    <Color x:Key="HoverColor">#FF2D2D42</Color>
    <Color x:Key="SelectedColor">#FF1A4A6E</Color>

    <!-- ── Override brushes ────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="SurfaceBrush"       Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceAltBrush"    Color="{StaticResource SurfaceAltColor}"/>
    <SolidColorBrush x:Key="BorderBrush"        Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush"   Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="HoverBrush"         Color="{StaticResource HoverColor}"/>
    <SolidColorBrush x:Key="SelectedBrush"      Color="{StaticResource SelectedColor}"/>

</ResourceDictionary>
